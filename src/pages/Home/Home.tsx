import React from 'react';

import s from './Home.module.scss';

interface Props {
  
}

const Home = (props: Props) => {
  return (
    <div className={s.home}>
      Home
    </div>
  )
}

export default Home
